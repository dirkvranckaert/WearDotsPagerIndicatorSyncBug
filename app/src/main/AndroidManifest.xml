<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bug.indicator.pager.poc.vranckaert.eu.dotspagerindicatorpagingbug" >

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="DotsPagerIndicatorBug"
        android:theme="@android:style/Theme.DeviceDefault" >
        <activity
            android:name=".DotsPagerIndicatorActivityWithBug"
            android:label="DPI - BUG" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DotsPagerIndicatorActivityWithoutBug"
            android:label="DPI - FIX" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
